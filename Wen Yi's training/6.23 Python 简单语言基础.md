
下载python
1 B
1kB = 1024 B
1MB = 1024KB
1GB =1024MB

使用conda：
使用不同的虚拟环境
conda create -n “名字” python=3.x
conda activate [环境]

基本语句：
声明变量 直接用等于号
print()
变量
input()
type()

int integer 整数
str string 字符串
list 列表:
1. append方法 加入到列表的末尾
类型转换 int()

for 循环语句
range(a, b) 左闭右开 长度为b-a
e.g.
`for i in range(1, 5)`
len() length 获取长度
sorted() 排序(默认从小到大)

冒号+缩进=代码块内
if和elif
逻辑符号 and or not xor
设计程序时要考虑边界情况

逻辑运算
and 与 两个都必须是真才返回真，否则返回假
or 或 两个之中有一个是真就返回真
not 返回反
xor(^) 异或 两个不同就返回真

变量名: 类型
声明类型而不直接赋值
e.g.
`n: list[int]`

`input().split()`
将一行输入按照某种方式分隔，返回元素是字符串的列表
`map(func, *iterable)`
对某个容器中的所有元素调用一遍func